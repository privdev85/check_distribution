from setuptools import setup

setup(name='compare_distributions',
      version='0.1',
      description='Compares input data to a number of different distributions',
      packages=['compare_distributions'],
      zip_safe=False,
      include_package_data = True,
      )
